# Tenká API vrstva – běží na běžném počítači lékaře.
#
# Záměrně tu NEJSOU torch, monai, pyvista, pyradiomics, scipy ani numpy.
# Veškerý výpočet dělá samostatný worker (repo ACL_graft_analysis, Source/worker).
# Kdyby se některá z těch závislostí vrátila, znamenalo by to, že se výpočet
# vrátil do tenké vrstvy a architektura se rozpadla.
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
sqlalchemy>=2.0.28
pydantic>=2.6.4
python-multipart>=0.0.9
httpx>=0.27.0
ruff>=0.4.0
